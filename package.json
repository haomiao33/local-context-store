{
  "name": "local-context-store",
  "version": "0.1.0",
  "description": "A local-first context store for coding agents such as Claude Code and Codex.",
  "type": "module",
  "bin": { "ctx": "./src/cli.js" },
  "files": ["src", "README.md", "LICENSE"],
  "scripts": {
    "build": "node --check src/db.js && node --check src/store.js && node --check src/cli.js && node --check src/mcp.js",
    "test": "node --test",
    "start": "node src/cli.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "better-sqlite3": "^12.4.1",
    "commander": "^14.0.0",
    "zod": "^4.1.5"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
