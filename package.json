{
  "name": "local-context-store",
  "version": "0.2.0-alpha.2",
  "description": "A local-first context store for coding agents such as Claude Code and Codex.",
  "type": "module",
  "bin": { "ctx": "./src/cli.js" },
  "files": ["src", "model", "README.md", "LICENSE"],
  "scripts": {
    "build": "node --check src/db.js && node --check src/store.js && node --check src/vector.js && node --check src/embedding.js && node --check src/hybrid.js && node --check src/cli.js && node --check src/mcp.js",
    "test": "node --test",
    "benchmark": "node benchmark/v02.js",
    "start": "node src/cli.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@modelcontextprotocol/sdk": "^1.17.5",
    "better-sqlite3": "^12.4.1",
    "commander": "^14.0.0",
    "zod": "^4.1.5"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
